spring.application.name=energy-producer
server.port=8083

# Open-Meteo current weather (Vienna). Must include current_weather=true so the
# response contains current_weather.weathercode, which WeatherService reads.
weather.api.url=https://api.open-meteo.com/v1/forecast?latitude=48.21&longitude=16.37&current_weather=true
